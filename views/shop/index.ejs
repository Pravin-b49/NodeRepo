<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>
        <%= pageTitle %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
        crossorigin="anonymous"></script>
</head>

<body class="bg-secondary ">
    <main class="main">
        <% if(message.length > 0) { %>
            <div class="alert alert-success"><%= message %></div>
        <% } %>
        <h3 class="text-center text-white "><u>Mobile Details</u></h3>
        <div class="row">
            <form action="/shop/<% if (editing) { %>edit-product<% } else { %>create-product<% } %>"
                method="post" class="col-lg-4 m-auto shadow-lg p-3 mb-5 bg-body-tertiary rounded">
                <input name="brand" value="<% if (editing) { %><%= product.brand %><% } %>" class="form-control mb-2"
                    placeholder="Enter Brand Name" />
                <input name="model_no" value="<% if (editing) { %><%= product.model_no %><% } %>"
                    class="form-control mb-2" placeholder="Enter Model no" />
                <input name="processor" value="<% if (editing) { %><%= product.processor %><% } %>"
                    class="form-control mb-2" placeholder="Enter Processor Name" />
                <input name="ram" value="<% if (editing) { %><%= product.ram %><% } %>" class="form-control mb-2"
                    placeholder="Enter RAM" />
                <input name="front_camera" value="<% if (editing) { %><%= product.front_camera %><% } %>"
                    class="form-control mb-2" placeholder="Enter Front Camera" />
                <input name="rear_camera" value="<% if (editing) { %><%= product.rear_camera %><% } %>"
                    class="form-control mb-2" placeholder="Enter Rear Camera" />
                <input type="number" name="price" class="form-control mb-2"
                    value="<% if (editing) { %><%= product.price %><% } %>" placeholder="Enter Price" />
                <input name="description" value="<% if (editing) { %><%= product.description %><% } %>"
                    class="form-control mb-2" placeholder="Enter Description" />
                <% if (editing) { %>
                    <input type="hidden" value="<%= product.id %>" name="productId">
                    <% } %>
                        <div class="inline">
                            <input type="Reset" class="btn btn-primary" />
                            <input type="Submit" class="btn btn-success"
                                value="<% if (editing) { %>Update<% } else { %>Submit<% } %>" />
                            <a href="/shop/product_list" class="btn btn-secondary">View Products</a>
                        </div>
            </form>
        </div>

    </main>
</body>

</html>